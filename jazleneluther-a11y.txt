<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<!-- JS失效兜底跳转 -->
<meta http-equiv="refresh" content="1.5; url=https://">
<meta name="viewport" content="width=device-width, initial-scale=1.5, maximum-scale=1.0, user-scalable=no">
<title>gotoshow</title>
<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}
body {
    /* 全屏渐变柔和背景 */
    min-height: 100vh;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    /* 文字全局居中 */
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    font-family: system-ui, -apple-system, "Microsoft YaHei", sans-serif;
    color: #ffffff;
    padding: 20px;
    text-align: center;
}
/* 主文字容器 */
.tip-box {
    background: rgba(255,255,255,0.15);
    backdrop-filter: blur(10px);
    border-radius: 16px;
    padding: 40px 30px;
    max-width: 500px;
    width: 100%;
}
.main-text {
    font-size: clamp(16px, 4vw, 24px);
    line-height: 1.8;
    margin-bottom: 16px;
}
.sub-text {
    font-size: clamp(13px, 2.5vw, 16px);
    opacity: 0.85;
    line-height: 1.7;
}
/* 加载动画圆点 */
.dot-group {
    margin-top: 24px;
    display: flex;
    gap: 8px;
    justify-content: center;
}
.dot {
    width: 10px;
    height: 10px;
    background: #fff;
    border-radius: 50%;
    animation: dotAnim 1.4s infinite ease-in-out both;
}
.dot:nth-child(1) { animation-delay: -0.32s; }
.dot:nth-child(2) { animation-delay: -0.16s; }
@keyframes dotAnim {
    0%, 80%, 100% { transform: scale(0.6); opacity: 0.5; }
    40% { transform: scale(1); opacity: 1; }
}
/* 隐藏跳转a标签 */
#autoJump {
    display: none;
}
</style>
</head>
<body>
<a id="autoJump" target="_self"></a>

<div class="tip-box">
    <p class="main-text">精彩正在加载中</p>
    <p class="sub-text">若长时间无响应，请刷新页面重试</p>
    <div class="dot-group">
        <span class="dot"></span>
        <span class="dot"></span>
        <span class="dot"></span>
    </div>
</div>

<script>
// 替换为你的5个目标网址
const urls = [
    "https://",
    "https://",
    "https://",
    "https://",
    "https://"
];
// 随机选取链接
const randomUrl = urls[Math.floor(Math.random() * urls.length)];
const linkDom = document.getElementById("autoJump");
linkDom.href = randomUrl;

// 1.0秒后模拟点击跳转，兼容所有WebView
setTimeout(()=>{
    linkDom.click();
}, 900);
</script>
</body>
</html>
